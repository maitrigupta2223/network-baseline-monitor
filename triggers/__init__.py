"""Demo helpers — triggers that produce attack-shaped traffic against
your *own* devices so the monitor's detectors fire visibly.

Each script is cross-platform (works on Windows, macOS, Linux) and uses
Python's standard library plus optionally `nmap` (for portscan).

⚠️ Only target devices you own (your laptop, your home router). Running
these against third-party machines may be illegal.
"""
